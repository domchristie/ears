<% more_feeds ||= nil %>

<%= render "list", id: "play_later", partial: "queues/episode", collection: episodes do |p| %>
  <%= p.content_for :empty, "Play later episodes appear here" %>

  <% if more_feeds %>
    <%= render "feeds/covers", feeds: more_feeds do %>
      <%= link_to "Play Later →", queue_path, data: {turbo_frame: "_top"}, class: "text-sm font-semibold uppercase tracking-wide before:absolute before:inset-0 before:z-10" %>
    <% end %>
  <% end %>
<% end %>
