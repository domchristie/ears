<!DOCTYPE html>
<html data-controller="player media-session" data-action="turbo:load->player#updateToggles">
  <head>
    <title><%= @title ? "#{@title} - All Ears" : "All Ears" %></title>
    <meta name="viewport" content="width=device-width,viewport-fit=cover,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="manifest" href="/manifest.json">
    <meta name="theme-color" content="#ffffff">
    <link rel="apple-touch-icon" href="/icon.png">
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>👂</text></svg>">

    <link href="https://api.fontshare.com/css?f[]=bespoke-sans@1,2&display=swap" rel="stylesheet">
    <link href='https://tools-static.wmflabs.org/fontcdn/css?family=Roboto+Mono:400&subset=latin' rel='stylesheet' type='text/css'>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <script src="//js.honeybadger.io/v4.0/honeybadger.min.js" type="text/javascript"></script>
    <script type="text/javascript">
      Honeybadger.configure({
        apiKey: '<%= ENV["HONEYBADGER_API_KEY"] %>',
        environment: '<%= Rails.env %>',
        revision: '<%= ENV["HEROKU_SLUG_COMMIT"] %>'
      });
    </script>
    <%= javascript_importmap_tags %>
  </head>

  <body class="mb-32 dark:bg-neutral-900 dark:text-neutral-100">
    <div class="fixed w-full top-0 pt-24 pointer-events-none transition-opacity opacity-0 turn-exit:opacity-100 turn-exit:delay-700">
      <%= inline_svg "icons/loading.svg", title: "Loading", class: "w-8 h-8 mx-auto text-blue-600 turn-exit:motion-safe:animate-spin" %>
    </div>

     <% if notice || alert %>
      <div class="p-2 text-center <%= notice ? "bg-green-50 text-green-900" : "bg-red-50 text-red-800" %>">
        <%= notice || alert %>
      </div>
    <% end %>

    <%= content_for :nav %>

    <main <%= turn_attributes %>>
      <%= yield %>
    </main>

    <% unless @hide_player %>
      <div data-turbo-permanent id="player_bar" class="fixed bottom-0 w-full min-h-[1px] bg-white/95 [@supports(backdrop-filter:blur(0))]:bg-white/75 [@supports(backdrop-filter:blur(0))]:backdrop-blur">
        <%= render "entries/players/audio", source: Current.entry.try(:resume_enclosure_url, Current.play) %>

        <div style="margin-bottom: env(safe-area-inset-bottom)"><%# Leave room for iOS home bar %>
          <%= turbo_frame_tag :player, src: (entry_player_path(Current.entry) if Current.entry) %>
        </div>
      </div>
    <% end %>

    <script data-turbo-eval="false">
    if (navigator.serviceWorker) {
      navigator.serviceWorker.register('/serviceworker.js', { scope: './' })
        .then(function () {
          console.log('[Page] Service worker registered!')
        })
    }
    </script>
  </body>
</html>
