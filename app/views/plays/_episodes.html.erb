<% more_feeds ||= nil %>

<%= render "list", id: "plays", partial: "episodes/episode", collection: episodes do |p| %>
  <%= p.content_for :empty, "Recently played episodes appear here" %>

  <% if more_feeds %>
    <%= render "feeds/covers", feeds: more_feeds do %>
      <%= link_to "Played Episodes →", plays_path, data: {turbo_frame: "_top"}, class: "text-sm font-semibold uppercase tracking-wide before:absolute before:inset-0 before:z-10" %>
    <% end %>
  <% end %>
<% end %>
