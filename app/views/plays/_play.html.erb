<div
  data-player-target="play"
  data-href="<%= play.entry&.enclosure_url %>"
  class="flex items-center <%= play_state_class_names(play) %>"
>
  <span class="<%= class_names("hidden played:flex items-center") %>">
    <%= icon :"check-circle", solid: true, class: "h-4 w-4 -ml-px mr-1 text-teal-500" %>
    Played
  </span>

  <span class="played:hidden flex items-center">
    <%= render(
      "timer",
      progress: play.progress,
      options: {
        aria: {hidden: true},
        data: {player_target: "timerIcon", href: play.entry&.enclosure_url},
        class: "h-4 w-4 -ml-px mr-1 text-neutral-300 started:text-blue-500",
        remaining: ({class: "started:text-blue-200"}),
        progress: ({class: "started:text-white"}),
        needle: ({class: "started:fill-blue-700"})
      }
    ) %>

    <%= render "plays/remaining_in_words", play: play %>
  </span>
</div>
