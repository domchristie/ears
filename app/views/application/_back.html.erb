<%= render(
  "nav_link",
  path: path,
  icon: :"arrow-sm-left",
  label: local_assigns[:text] || "Back",
  color: local_assigns[:color] || :default,
  data: {controller: "back", action: "back#back"}
) %>
